import { BoardSize, Setting } from './setting'
import {
  Move,
  GameState,
  GameAction,
  ActionType,
  ActionReset,
  ActionMove,
  ActionGameOver,
  ActionNextTurn,
} from './game'

export { ActionType }
export type { Move, GameState, GameAction, ActionReset, ActionMove, ActionGameOver, ActionNextTurn }
export type { Setting, BoardSize }
